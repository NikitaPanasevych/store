const initialState = {
	loading: false,
	posts: [],
	error: '',
};

/*const getPostsSlice = createSlice({
    name: 'getPosts',
    initialState,
    reducers: {
        getPostsRequest: (state) => {
            state.loading = true;
        }
    },*/
